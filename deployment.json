{
  "_comment": "Apps Script deployment that tagged releases update in place. Its ID is what the Marketplace SDK listing (#4) points at, so it must stay stable. Empty until the first tagged release creates it -- then commit the ID tools/release.sh prints.",
  "deploymentId": ""
}
